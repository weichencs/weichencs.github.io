// Minimal JS (optional)
// You can expand with "copy bibtex" buttons, image zoom, etc.

document.addEventListener("DOMContentLoaded", () => {
  // Example: add copy button for BibTeX if you want later.
});
